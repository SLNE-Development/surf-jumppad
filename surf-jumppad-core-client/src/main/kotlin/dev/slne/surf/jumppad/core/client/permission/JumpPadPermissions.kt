package dev.slne.surf.jumppad.core.client.permission

/**
 * The permissions guarding jump pad functionality.
 */
object JumpPadPermissions {
    const val PREFIX = "surf.jump-pad"
    const val COMMAND_PREFIX = "$PREFIX.command"

    const val COMMAND_JUMP_PAD_GENERIC = "$COMMAND_PREFIX.jump-pad"
}
